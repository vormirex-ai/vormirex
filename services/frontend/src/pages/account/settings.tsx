import { SettingsContainer } from "@/components/dashboard/settings/settings-container";

export default function SettingsPage() {
  return (
    <main className="min-h-screen p-1 lg:p-10">
      <SettingsContainer />
    </main>
  );
}